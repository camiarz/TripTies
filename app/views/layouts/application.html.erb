<!DOCTYPE html>
<html>
  <head>
    <title>TripTies</title>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
  </head>

  <body>
    <% unless controller_name == "pages" && action_name == "home"%>
      <%= render 'shared/navbar' %>
    <% end %>
      <%= yield %>

      <%# Do not remove this class, helps give a spacing between the bottom of the page and footer %>
      <div class="footer-push"></div>

        <%= render 'shared/footer' unless request.fullpath == '/' || request.fullpath.include?('users') %>
  </body>
</html>
